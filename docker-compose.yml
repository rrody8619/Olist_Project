version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: olist_inference_api
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    restart: always